
def generate_mcqs_from_transcript(transcript_segments: list[str]) -> list[dict]:
    print("🧠 Generating MCQs from transcript...")
    # Replace with LLM logic later
    return [
        {
            "question": "What is the main topic of this segment?",
            "options": ["Topic A", "Topic B", "Topic C", "Topic D"],
            "answer": "Topic A"
        }
        for segment in transcript_segments
    ]


